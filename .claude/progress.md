# Progress

**Current milestone: 1 — Skeleton + gate** (complete)

Scaffold, plan, research, pure logic modules with tests, CI, private repo with `main`
protected. No product features built.

**Next: milestone 2 — The hierarchy.** Build `scripts/build-dataset/` stages 1–3 so
`public/data/graph.json` contains a real MusicBrainz genre tree instead of the sample
dataset. Start at `scripts/build-dataset/index.ts`; the hard part is the HTML scrape in
stage 2 (see `.claude/project-context.md`).

Milestones are listed in `plan.md`. Open work is in `docs/future.md`.
