# Progress

**Current milestone: 5 — Find your way around** (complete)

The left filter panel is real: search by name, multi-select with chips,
hide-everything-else (via the already-tested `resolveFilter` hard gate in `lod.ts`),
collapsible, and every filtered view is a shareable `?filter=` URL. The core
navigation loop — map, zoom, focus, fan, panel, filter, deep links — is complete.

**Next: milestone 6 — Make it enjoyable.** Pipeline stage 6 (Deezer 30s preview
MP3s + track links) and the audio player in the panel; touch and mobile (pinch-zoom,
tap-to-drill, bottom-sheet panel); motion and polish pass. Also queued from the
backlog: the colour-family scheme (rock/electronic hue collision, 131 singleton
roots) fits naturally in the polish pass.

Milestones are listed in `plan.md`. Open work is in `docs/future.md`.
